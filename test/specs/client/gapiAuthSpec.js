var expect = chai.expect;

describe('Google authentication module', function() {
	describe('Authentication url retrieval', function() {
		it('retrieves an authentication url from the Pocument server', function() {
			
		});
	});
});